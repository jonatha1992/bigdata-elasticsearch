# Generate independent random alphanumeric values; never commit the real .env.
# The setup request expects alphanumeric passwords (no JSON escape characters).
ELASTIC_PASSWORD=
KIBANA_PASSWORD=
# Each encryption key must contain at least 32 characters.
KIBANA_SECURITY_KEY=
KIBANA_OBJECTS_KEY=
KIBANA_REPORTING_KEY=
